package agi.foundation.compatibility;

/**
 * Provides the base class for value types.
 * 
 * Marker interface for value types to allow correct handling of default(T).
 */
public interface ValueType {}
