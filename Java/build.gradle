subprojects {
    apply plugin: 'java'

    repositories {
       mavenCentral()
    }

    sourceCompatibility = 1.6
    targetCompatibility = 1.6
    version = '1.0'

    processResources {
        includeEmptyDirs = false
    }
}
