{
    "title": "Reference",
    "description": "Represents a link to another property.  References can be used to specify that two properties on different objects are in fact, the same property.  This also has the added benefit of cutting down on CZML file size.",
    "type": [ "object" ],
    "items": { "$ref": "#" },
    "properties": {
        "reference": {
            "$ref": "StringValue.jsonschema",
            "description": "The id of the object which contains the referenced property.",
            "czmlValue": true
        }
    }
}
