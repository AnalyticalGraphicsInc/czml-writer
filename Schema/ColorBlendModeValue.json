{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://analyticalgraphicsinc.github.io/czml-writer/Schema/ColorBlendModeValue.json",
    "title": "ColorBlendMode",
    "description": "Defines different modes for blending between a target color and an entity's source color.",
    "type": "string",
    "oneOf": [
        {
            "const": "HIGHLIGHT",
            "description": "Multiplies the source color by the target color."
        },
        {
            "const": "REPLACE",
            "description": "Replaces the source color with the target color."
        },
        {
            "const": "MIX",
            "description": "Blends the source color and target color together."
        }
    ]
}