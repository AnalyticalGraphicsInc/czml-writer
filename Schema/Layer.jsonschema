{
    "title": "Layer",
    "description": "A czml document or link to another layer",
    "type": ["array", "object"],
    "items": { "$ref": "#" },
    "properties": {
		"show": {
            "$ref": "Boolean.jsonschema",
            "description": "Whether or not the layer is shown."
        },
        "layerUrl": {
            "$ref": "String.jsonschema",
            "description": "The url where the czml document resides."
        }
    }
}