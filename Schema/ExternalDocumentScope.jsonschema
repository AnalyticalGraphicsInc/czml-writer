{
    "title": "ExternalDocumentScope",
    "description": "Defines the scope of the document.  It controls whether the document is shared or private.",
    "type": ["array", "object", "string"],
    "items": { "$ref": "#" },
    "properties": {
        "scope": {
            "$ref": "ExternalDocumentScopeValue.jsonschema",
            "description": "The document's scope.  Valid values are \"SHARED\" and \"PRIVATE\".",
            "czmlValue": true
        }
    }
}